name: spun_book_domain
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  user_manager_domain:
    git:
      url: https://github.com/precastapp/user_manager.git
      path: packages/user_manager_domain
      ref: main

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
